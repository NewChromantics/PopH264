name: Build Linux

on: 
  push:
    branches:
      - ActionTest

jobs:
  buildlinux:
    runs-on: ubuntu-latest
    name: A job to build a Linux executable
    steps:
      # To use this repository's private action,
      # you must check out the repository
      - name: Checkout
        uses: actions/checkout@v2
        with:
          submodules: true
      - name: Build step
        uses: ./.github/actions/ # Uses an action in the root directory
        id: linux-build
      # TODO: Get some confirmation of the results of the action 
      - name: Get the results
        run: echo "Hello World"
